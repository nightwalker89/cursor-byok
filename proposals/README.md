# Proposals & Notes

Scratch design proposals and reverse-engineering notes that are **not** part of
the shipped extension and may be out of date. Nothing here is authoritative — the
source of truth is the code under `src/` and the docs under [`docs/`](../docs/).

- `minimal-architecture.md` — an unimplemented proposal to collapse the
  server/state layers into the hook.

The installer (`scripts/install-cursor.js`) excludes this directory.
